// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/longgggwwww/hrm-ms-hr/ent/appointmenthistory"
	"github.com/longgggwwww/hrm-ms-hr/ent/employee"
	"github.com/longgggwwww/hrm-ms-hr/ent/predicate"
)

// AppointmentHistoryQuery is the builder for querying AppointmentHistory entities.
type AppointmentHistoryQuery struct {
	config
	ctx          *QueryContext
	order        []appointmenthistory.OrderOption
	inters       []Interceptor
	predicates   []predicate.AppointmentHistory
	withEmployee *EmployeeQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the AppointmentHistoryQuery builder.
func (ahq *AppointmentHistoryQuery) Where(ps ...predicate.AppointmentHistory) *AppointmentHistoryQuery {
	ahq.predicates = append(ahq.predicates, ps...)
	return ahq
}

// Limit the number of records to be returned by this query.
func (ahq *AppointmentHistoryQuery) Limit(limit int) *AppointmentHistoryQuery {
	ahq.ctx.Limit = &limit
	return ahq
}

// Offset to start from.
func (ahq *AppointmentHistoryQuery) Offset(offset int) *AppointmentHistoryQuery {
	ahq.ctx.Offset = &offset
	return ahq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (ahq *AppointmentHistoryQuery) Unique(unique bool) *AppointmentHistoryQuery {
	ahq.ctx.Unique = &unique
	return ahq
}

// Order specifies how the records should be ordered.
func (ahq *AppointmentHistoryQuery) Order(o ...appointmenthistory.OrderOption) *AppointmentHistoryQuery {
	ahq.order = append(ahq.order, o...)
	return ahq
}

// QueryEmployee chains the current query on the "employee" edge.
func (ahq *AppointmentHistoryQuery) QueryEmployee() *EmployeeQuery {
	query := (&EmployeeClient{config: ahq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := ahq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := ahq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(appointmenthistory.Table, appointmenthistory.FieldID, selector),
			sqlgraph.To(employee.Table, employee.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, appointmenthistory.EmployeeTable, appointmenthistory.EmployeeColumn),
		)
		fromU = sqlgraph.SetNeighbors(ahq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first AppointmentHistory entity from the query.
// Returns a *NotFoundError when no AppointmentHistory was found.
func (ahq *AppointmentHistoryQuery) First(ctx context.Context) (*AppointmentHistory, error) {
	nodes, err := ahq.Limit(1).All(setContextOp(ctx, ahq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{appointmenthistory.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (ahq *AppointmentHistoryQuery) FirstX(ctx context.Context) *AppointmentHistory {
	node, err := ahq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first AppointmentHistory ID from the query.
// Returns a *NotFoundError when no AppointmentHistory ID was found.
func (ahq *AppointmentHistoryQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = ahq.Limit(1).IDs(setContextOp(ctx, ahq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{appointmenthistory.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (ahq *AppointmentHistoryQuery) FirstIDX(ctx context.Context) int {
	id, err := ahq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single AppointmentHistory entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one AppointmentHistory entity is found.
// Returns a *NotFoundError when no AppointmentHistory entities are found.
func (ahq *AppointmentHistoryQuery) Only(ctx context.Context) (*AppointmentHistory, error) {
	nodes, err := ahq.Limit(2).All(setContextOp(ctx, ahq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{appointmenthistory.Label}
	default:
		return nil, &NotSingularError{appointmenthistory.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (ahq *AppointmentHistoryQuery) OnlyX(ctx context.Context) *AppointmentHistory {
	node, err := ahq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only AppointmentHistory ID in the query.
// Returns a *NotSingularError when more than one AppointmentHistory ID is found.
// Returns a *NotFoundError when no entities are found.
func (ahq *AppointmentHistoryQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = ahq.Limit(2).IDs(setContextOp(ctx, ahq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{appointmenthistory.Label}
	default:
		err = &NotSingularError{appointmenthistory.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (ahq *AppointmentHistoryQuery) OnlyIDX(ctx context.Context) int {
	id, err := ahq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of AppointmentHistories.
func (ahq *AppointmentHistoryQuery) All(ctx context.Context) ([]*AppointmentHistory, error) {
	ctx = setContextOp(ctx, ahq.ctx, ent.OpQueryAll)
	if err := ahq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*AppointmentHistory, *AppointmentHistoryQuery]()
	return withInterceptors[[]*AppointmentHistory](ctx, ahq, qr, ahq.inters)
}

// AllX is like All, but panics if an error occurs.
func (ahq *AppointmentHistoryQuery) AllX(ctx context.Context) []*AppointmentHistory {
	nodes, err := ahq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of AppointmentHistory IDs.
func (ahq *AppointmentHistoryQuery) IDs(ctx context.Context) (ids []int, err error) {
	if ahq.ctx.Unique == nil && ahq.path != nil {
		ahq.Unique(true)
	}
	ctx = setContextOp(ctx, ahq.ctx, ent.OpQueryIDs)
	if err = ahq.Select(appointmenthistory.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (ahq *AppointmentHistoryQuery) IDsX(ctx context.Context) []int {
	ids, err := ahq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (ahq *AppointmentHistoryQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, ahq.ctx, ent.OpQueryCount)
	if err := ahq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, ahq, querierCount[*AppointmentHistoryQuery](), ahq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (ahq *AppointmentHistoryQuery) CountX(ctx context.Context) int {
	count, err := ahq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (ahq *AppointmentHistoryQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, ahq.ctx, ent.OpQueryExist)
	switch _, err := ahq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (ahq *AppointmentHistoryQuery) ExistX(ctx context.Context) bool {
	exist, err := ahq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the AppointmentHistoryQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (ahq *AppointmentHistoryQuery) Clone() *AppointmentHistoryQuery {
	if ahq == nil {
		return nil
	}
	return &AppointmentHistoryQuery{
		config:       ahq.config,
		ctx:          ahq.ctx.Clone(),
		order:        append([]appointmenthistory.OrderOption{}, ahq.order...),
		inters:       append([]Interceptor{}, ahq.inters...),
		predicates:   append([]predicate.AppointmentHistory{}, ahq.predicates...),
		withEmployee: ahq.withEmployee.Clone(),
		// clone intermediate query.
		sql:  ahq.sql.Clone(),
		path: ahq.path,
	}
}

// WithEmployee tells the query-builder to eager-load the nodes that are connected to
// the "employee" edge. The optional arguments are used to configure the query builder of the edge.
func (ahq *AppointmentHistoryQuery) WithEmployee(opts ...func(*EmployeeQuery)) *AppointmentHistoryQuery {
	query := (&EmployeeClient{config: ahq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	ahq.withEmployee = query
	return ahq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		EmployeeID int `json:"employee_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.AppointmentHistory.Query().
//		GroupBy(appointmenthistory.FieldEmployeeID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (ahq *AppointmentHistoryQuery) GroupBy(field string, fields ...string) *AppointmentHistoryGroupBy {
	ahq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &AppointmentHistoryGroupBy{build: ahq}
	grbuild.flds = &ahq.ctx.Fields
	grbuild.label = appointmenthistory.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		EmployeeID int `json:"employee_id,omitempty"`
//	}
//
//	client.AppointmentHistory.Query().
//		Select(appointmenthistory.FieldEmployeeID).
//		Scan(ctx, &v)
func (ahq *AppointmentHistoryQuery) Select(fields ...string) *AppointmentHistorySelect {
	ahq.ctx.Fields = append(ahq.ctx.Fields, fields...)
	sbuild := &AppointmentHistorySelect{AppointmentHistoryQuery: ahq}
	sbuild.label = appointmenthistory.Label
	sbuild.flds, sbuild.scan = &ahq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a AppointmentHistorySelect configured with the given aggregations.
func (ahq *AppointmentHistoryQuery) Aggregate(fns ...AggregateFunc) *AppointmentHistorySelect {
	return ahq.Select().Aggregate(fns...)
}

func (ahq *AppointmentHistoryQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range ahq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, ahq); err != nil {
				return err
			}
		}
	}
	for _, f := range ahq.ctx.Fields {
		if !appointmenthistory.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if ahq.path != nil {
		prev, err := ahq.path(ctx)
		if err != nil {
			return err
		}
		ahq.sql = prev
	}
	return nil
}

func (ahq *AppointmentHistoryQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*AppointmentHistory, error) {
	var (
		nodes       = []*AppointmentHistory{}
		_spec       = ahq.querySpec()
		loadedTypes = [1]bool{
			ahq.withEmployee != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*AppointmentHistory).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &AppointmentHistory{config: ahq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, ahq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := ahq.withEmployee; query != nil {
		if err := ahq.loadEmployee(ctx, query, nodes, nil,
			func(n *AppointmentHistory, e *Employee) { n.Edges.Employee = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (ahq *AppointmentHistoryQuery) loadEmployee(ctx context.Context, query *EmployeeQuery, nodes []*AppointmentHistory, init func(*AppointmentHistory), assign func(*AppointmentHistory, *Employee)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*AppointmentHistory)
	for i := range nodes {
		fk := nodes[i].EmployeeID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(employee.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "employee_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (ahq *AppointmentHistoryQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := ahq.querySpec()
	_spec.Node.Columns = ahq.ctx.Fields
	if len(ahq.ctx.Fields) > 0 {
		_spec.Unique = ahq.ctx.Unique != nil && *ahq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, ahq.driver, _spec)
}

func (ahq *AppointmentHistoryQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(appointmenthistory.Table, appointmenthistory.Columns, sqlgraph.NewFieldSpec(appointmenthistory.FieldID, field.TypeInt))
	_spec.From = ahq.sql
	if unique := ahq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if ahq.path != nil {
		_spec.Unique = true
	}
	if fields := ahq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, appointmenthistory.FieldID)
		for i := range fields {
			if fields[i] != appointmenthistory.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if ahq.withEmployee != nil {
			_spec.Node.AddColumnOnce(appointmenthistory.FieldEmployeeID)
		}
	}
	if ps := ahq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := ahq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := ahq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := ahq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (ahq *AppointmentHistoryQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(ahq.driver.Dialect())
	t1 := builder.Table(appointmenthistory.Table)
	columns := ahq.ctx.Fields
	if len(columns) == 0 {
		columns = appointmenthistory.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if ahq.sql != nil {
		selector = ahq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if ahq.ctx.Unique != nil && *ahq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range ahq.predicates {
		p(selector)
	}
	for _, p := range ahq.order {
		p(selector)
	}
	if offset := ahq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := ahq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// AppointmentHistoryGroupBy is the group-by builder for AppointmentHistory entities.
type AppointmentHistoryGroupBy struct {
	selector
	build *AppointmentHistoryQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (ahgb *AppointmentHistoryGroupBy) Aggregate(fns ...AggregateFunc) *AppointmentHistoryGroupBy {
	ahgb.fns = append(ahgb.fns, fns...)
	return ahgb
}

// Scan applies the selector query and scans the result into the given value.
func (ahgb *AppointmentHistoryGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ahgb.build.ctx, ent.OpQueryGroupBy)
	if err := ahgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AppointmentHistoryQuery, *AppointmentHistoryGroupBy](ctx, ahgb.build, ahgb, ahgb.build.inters, v)
}

func (ahgb *AppointmentHistoryGroupBy) sqlScan(ctx context.Context, root *AppointmentHistoryQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(ahgb.fns))
	for _, fn := range ahgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*ahgb.flds)+len(ahgb.fns))
		for _, f := range *ahgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*ahgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ahgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// AppointmentHistorySelect is the builder for selecting fields of AppointmentHistory entities.
type AppointmentHistorySelect struct {
	*AppointmentHistoryQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ahs *AppointmentHistorySelect) Aggregate(fns ...AggregateFunc) *AppointmentHistorySelect {
	ahs.fns = append(ahs.fns, fns...)
	return ahs
}

// Scan applies the selector query and scans the result into the given value.
func (ahs *AppointmentHistorySelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ahs.ctx, ent.OpQuerySelect)
	if err := ahs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AppointmentHistoryQuery, *AppointmentHistorySelect](ctx, ahs.AppointmentHistoryQuery, ahs, ahs.inters, v)
}

func (ahs *AppointmentHistorySelect) sqlScan(ctx context.Context, root *AppointmentHistoryQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ahs.fns))
	for _, fn := range ahs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ahs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ahs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
