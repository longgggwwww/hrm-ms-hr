// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/longgggwwww/hrm-ms-hr/ent/leaveapproval"
	"github.com/longgggwwww/hrm-ms-hr/ent/predicate"
)

// LeaveApprovalQuery is the builder for querying LeaveApproval entities.
type LeaveApprovalQuery struct {
	config
	ctx        *QueryContext
	order      []leaveapproval.OrderOption
	inters     []Interceptor
	predicates []predicate.LeaveApproval
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the LeaveApprovalQuery builder.
func (laq *LeaveApprovalQuery) Where(ps ...predicate.LeaveApproval) *LeaveApprovalQuery {
	laq.predicates = append(laq.predicates, ps...)
	return laq
}

// Limit the number of records to be returned by this query.
func (laq *LeaveApprovalQuery) Limit(limit int) *LeaveApprovalQuery {
	laq.ctx.Limit = &limit
	return laq
}

// Offset to start from.
func (laq *LeaveApprovalQuery) Offset(offset int) *LeaveApprovalQuery {
	laq.ctx.Offset = &offset
	return laq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (laq *LeaveApprovalQuery) Unique(unique bool) *LeaveApprovalQuery {
	laq.ctx.Unique = &unique
	return laq
}

// Order specifies how the records should be ordered.
func (laq *LeaveApprovalQuery) Order(o ...leaveapproval.OrderOption) *LeaveApprovalQuery {
	laq.order = append(laq.order, o...)
	return laq
}

// First returns the first LeaveApproval entity from the query.
// Returns a *NotFoundError when no LeaveApproval was found.
func (laq *LeaveApprovalQuery) First(ctx context.Context) (*LeaveApproval, error) {
	nodes, err := laq.Limit(1).All(setContextOp(ctx, laq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{leaveapproval.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (laq *LeaveApprovalQuery) FirstX(ctx context.Context) *LeaveApproval {
	node, err := laq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first LeaveApproval ID from the query.
// Returns a *NotFoundError when no LeaveApproval ID was found.
func (laq *LeaveApprovalQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = laq.Limit(1).IDs(setContextOp(ctx, laq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{leaveapproval.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (laq *LeaveApprovalQuery) FirstIDX(ctx context.Context) int {
	id, err := laq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single LeaveApproval entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one LeaveApproval entity is found.
// Returns a *NotFoundError when no LeaveApproval entities are found.
func (laq *LeaveApprovalQuery) Only(ctx context.Context) (*LeaveApproval, error) {
	nodes, err := laq.Limit(2).All(setContextOp(ctx, laq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{leaveapproval.Label}
	default:
		return nil, &NotSingularError{leaveapproval.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (laq *LeaveApprovalQuery) OnlyX(ctx context.Context) *LeaveApproval {
	node, err := laq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only LeaveApproval ID in the query.
// Returns a *NotSingularError when more than one LeaveApproval ID is found.
// Returns a *NotFoundError when no entities are found.
func (laq *LeaveApprovalQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = laq.Limit(2).IDs(setContextOp(ctx, laq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{leaveapproval.Label}
	default:
		err = &NotSingularError{leaveapproval.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (laq *LeaveApprovalQuery) OnlyIDX(ctx context.Context) int {
	id, err := laq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of LeaveApprovals.
func (laq *LeaveApprovalQuery) All(ctx context.Context) ([]*LeaveApproval, error) {
	ctx = setContextOp(ctx, laq.ctx, ent.OpQueryAll)
	if err := laq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*LeaveApproval, *LeaveApprovalQuery]()
	return withInterceptors[[]*LeaveApproval](ctx, laq, qr, laq.inters)
}

// AllX is like All, but panics if an error occurs.
func (laq *LeaveApprovalQuery) AllX(ctx context.Context) []*LeaveApproval {
	nodes, err := laq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of LeaveApproval IDs.
func (laq *LeaveApprovalQuery) IDs(ctx context.Context) (ids []int, err error) {
	if laq.ctx.Unique == nil && laq.path != nil {
		laq.Unique(true)
	}
	ctx = setContextOp(ctx, laq.ctx, ent.OpQueryIDs)
	if err = laq.Select(leaveapproval.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (laq *LeaveApprovalQuery) IDsX(ctx context.Context) []int {
	ids, err := laq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (laq *LeaveApprovalQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, laq.ctx, ent.OpQueryCount)
	if err := laq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, laq, querierCount[*LeaveApprovalQuery](), laq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (laq *LeaveApprovalQuery) CountX(ctx context.Context) int {
	count, err := laq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (laq *LeaveApprovalQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, laq.ctx, ent.OpQueryExist)
	switch _, err := laq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (laq *LeaveApprovalQuery) ExistX(ctx context.Context) bool {
	exist, err := laq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the LeaveApprovalQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (laq *LeaveApprovalQuery) Clone() *LeaveApprovalQuery {
	if laq == nil {
		return nil
	}
	return &LeaveApprovalQuery{
		config:     laq.config,
		ctx:        laq.ctx.Clone(),
		order:      append([]leaveapproval.OrderOption{}, laq.order...),
		inters:     append([]Interceptor{}, laq.inters...),
		predicates: append([]predicate.LeaveApproval{}, laq.predicates...),
		// clone intermediate query.
		sql:  laq.sql.Clone(),
		path: laq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Comment string `json:"comment,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.LeaveApproval.Query().
//		GroupBy(leaveapproval.FieldComment).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (laq *LeaveApprovalQuery) GroupBy(field string, fields ...string) *LeaveApprovalGroupBy {
	laq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &LeaveApprovalGroupBy{build: laq}
	grbuild.flds = &laq.ctx.Fields
	grbuild.label = leaveapproval.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Comment string `json:"comment,omitempty"`
//	}
//
//	client.LeaveApproval.Query().
//		Select(leaveapproval.FieldComment).
//		Scan(ctx, &v)
func (laq *LeaveApprovalQuery) Select(fields ...string) *LeaveApprovalSelect {
	laq.ctx.Fields = append(laq.ctx.Fields, fields...)
	sbuild := &LeaveApprovalSelect{LeaveApprovalQuery: laq}
	sbuild.label = leaveapproval.Label
	sbuild.flds, sbuild.scan = &laq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a LeaveApprovalSelect configured with the given aggregations.
func (laq *LeaveApprovalQuery) Aggregate(fns ...AggregateFunc) *LeaveApprovalSelect {
	return laq.Select().Aggregate(fns...)
}

func (laq *LeaveApprovalQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range laq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, laq); err != nil {
				return err
			}
		}
	}
	for _, f := range laq.ctx.Fields {
		if !leaveapproval.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if laq.path != nil {
		prev, err := laq.path(ctx)
		if err != nil {
			return err
		}
		laq.sql = prev
	}
	return nil
}

func (laq *LeaveApprovalQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*LeaveApproval, error) {
	var (
		nodes = []*LeaveApproval{}
		_spec = laq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*LeaveApproval).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &LeaveApproval{config: laq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, laq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (laq *LeaveApprovalQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := laq.querySpec()
	_spec.Node.Columns = laq.ctx.Fields
	if len(laq.ctx.Fields) > 0 {
		_spec.Unique = laq.ctx.Unique != nil && *laq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, laq.driver, _spec)
}

func (laq *LeaveApprovalQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(leaveapproval.Table, leaveapproval.Columns, sqlgraph.NewFieldSpec(leaveapproval.FieldID, field.TypeInt))
	_spec.From = laq.sql
	if unique := laq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if laq.path != nil {
		_spec.Unique = true
	}
	if fields := laq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, leaveapproval.FieldID)
		for i := range fields {
			if fields[i] != leaveapproval.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := laq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := laq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := laq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := laq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (laq *LeaveApprovalQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(laq.driver.Dialect())
	t1 := builder.Table(leaveapproval.Table)
	columns := laq.ctx.Fields
	if len(columns) == 0 {
		columns = leaveapproval.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if laq.sql != nil {
		selector = laq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if laq.ctx.Unique != nil && *laq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range laq.predicates {
		p(selector)
	}
	for _, p := range laq.order {
		p(selector)
	}
	if offset := laq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := laq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// LeaveApprovalGroupBy is the group-by builder for LeaveApproval entities.
type LeaveApprovalGroupBy struct {
	selector
	build *LeaveApprovalQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (lagb *LeaveApprovalGroupBy) Aggregate(fns ...AggregateFunc) *LeaveApprovalGroupBy {
	lagb.fns = append(lagb.fns, fns...)
	return lagb
}

// Scan applies the selector query and scans the result into the given value.
func (lagb *LeaveApprovalGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, lagb.build.ctx, ent.OpQueryGroupBy)
	if err := lagb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*LeaveApprovalQuery, *LeaveApprovalGroupBy](ctx, lagb.build, lagb, lagb.build.inters, v)
}

func (lagb *LeaveApprovalGroupBy) sqlScan(ctx context.Context, root *LeaveApprovalQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(lagb.fns))
	for _, fn := range lagb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*lagb.flds)+len(lagb.fns))
		for _, f := range *lagb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*lagb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := lagb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// LeaveApprovalSelect is the builder for selecting fields of LeaveApproval entities.
type LeaveApprovalSelect struct {
	*LeaveApprovalQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (las *LeaveApprovalSelect) Aggregate(fns ...AggregateFunc) *LeaveApprovalSelect {
	las.fns = append(las.fns, fns...)
	return las
}

// Scan applies the selector query and scans the result into the given value.
func (las *LeaveApprovalSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, las.ctx, ent.OpQuerySelect)
	if err := las.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*LeaveApprovalQuery, *LeaveApprovalSelect](ctx, las.LeaveApprovalQuery, las, las.inters, v)
}

func (las *LeaveApprovalSelect) sqlScan(ctx context.Context, root *LeaveApprovalQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(las.fns))
	for _, fn := range las.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*las.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := las.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
