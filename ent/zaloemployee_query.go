// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/longgggwwww/hrm-ms-hr/ent/employee"
	"github.com/longgggwwww/hrm-ms-hr/ent/predicate"
	"github.com/longgggwwww/hrm-ms-hr/ent/zaloemployee"
)

// ZaloEmployeeQuery is the builder for querying ZaloEmployee entities.
type ZaloEmployeeQuery struct {
	config
	ctx          *QueryContext
	order        []zaloemployee.OrderOption
	inters       []Interceptor
	predicates   []predicate.ZaloEmployee
	withEmployee *EmployeeQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ZaloEmployeeQuery builder.
func (zeq *ZaloEmployeeQuery) Where(ps ...predicate.ZaloEmployee) *ZaloEmployeeQuery {
	zeq.predicates = append(zeq.predicates, ps...)
	return zeq
}

// Limit the number of records to be returned by this query.
func (zeq *ZaloEmployeeQuery) Limit(limit int) *ZaloEmployeeQuery {
	zeq.ctx.Limit = &limit
	return zeq
}

// Offset to start from.
func (zeq *ZaloEmployeeQuery) Offset(offset int) *ZaloEmployeeQuery {
	zeq.ctx.Offset = &offset
	return zeq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (zeq *ZaloEmployeeQuery) Unique(unique bool) *ZaloEmployeeQuery {
	zeq.ctx.Unique = &unique
	return zeq
}

// Order specifies how the records should be ordered.
func (zeq *ZaloEmployeeQuery) Order(o ...zaloemployee.OrderOption) *ZaloEmployeeQuery {
	zeq.order = append(zeq.order, o...)
	return zeq
}

// QueryEmployee chains the current query on the "employee" edge.
func (zeq *ZaloEmployeeQuery) QueryEmployee() *EmployeeQuery {
	query := (&EmployeeClient{config: zeq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := zeq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := zeq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(zaloemployee.Table, zaloemployee.FieldID, selector),
			sqlgraph.To(employee.Table, employee.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, true, zaloemployee.EmployeeTable, zaloemployee.EmployeeColumn),
		)
		fromU = sqlgraph.SetNeighbors(zeq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first ZaloEmployee entity from the query.
// Returns a *NotFoundError when no ZaloEmployee was found.
func (zeq *ZaloEmployeeQuery) First(ctx context.Context) (*ZaloEmployee, error) {
	nodes, err := zeq.Limit(1).All(setContextOp(ctx, zeq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{zaloemployee.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (zeq *ZaloEmployeeQuery) FirstX(ctx context.Context) *ZaloEmployee {
	node, err := zeq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ZaloEmployee ID from the query.
// Returns a *NotFoundError when no ZaloEmployee ID was found.
func (zeq *ZaloEmployeeQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = zeq.Limit(1).IDs(setContextOp(ctx, zeq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{zaloemployee.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (zeq *ZaloEmployeeQuery) FirstIDX(ctx context.Context) int {
	id, err := zeq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ZaloEmployee entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ZaloEmployee entity is found.
// Returns a *NotFoundError when no ZaloEmployee entities are found.
func (zeq *ZaloEmployeeQuery) Only(ctx context.Context) (*ZaloEmployee, error) {
	nodes, err := zeq.Limit(2).All(setContextOp(ctx, zeq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{zaloemployee.Label}
	default:
		return nil, &NotSingularError{zaloemployee.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (zeq *ZaloEmployeeQuery) OnlyX(ctx context.Context) *ZaloEmployee {
	node, err := zeq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ZaloEmployee ID in the query.
// Returns a *NotSingularError when more than one ZaloEmployee ID is found.
// Returns a *NotFoundError when no entities are found.
func (zeq *ZaloEmployeeQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = zeq.Limit(2).IDs(setContextOp(ctx, zeq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{zaloemployee.Label}
	default:
		err = &NotSingularError{zaloemployee.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (zeq *ZaloEmployeeQuery) OnlyIDX(ctx context.Context) int {
	id, err := zeq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ZaloEmployees.
func (zeq *ZaloEmployeeQuery) All(ctx context.Context) ([]*ZaloEmployee, error) {
	ctx = setContextOp(ctx, zeq.ctx, ent.OpQueryAll)
	if err := zeq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ZaloEmployee, *ZaloEmployeeQuery]()
	return withInterceptors[[]*ZaloEmployee](ctx, zeq, qr, zeq.inters)
}

// AllX is like All, but panics if an error occurs.
func (zeq *ZaloEmployeeQuery) AllX(ctx context.Context) []*ZaloEmployee {
	nodes, err := zeq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ZaloEmployee IDs.
func (zeq *ZaloEmployeeQuery) IDs(ctx context.Context) (ids []int, err error) {
	if zeq.ctx.Unique == nil && zeq.path != nil {
		zeq.Unique(true)
	}
	ctx = setContextOp(ctx, zeq.ctx, ent.OpQueryIDs)
	if err = zeq.Select(zaloemployee.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (zeq *ZaloEmployeeQuery) IDsX(ctx context.Context) []int {
	ids, err := zeq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (zeq *ZaloEmployeeQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, zeq.ctx, ent.OpQueryCount)
	if err := zeq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, zeq, querierCount[*ZaloEmployeeQuery](), zeq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (zeq *ZaloEmployeeQuery) CountX(ctx context.Context) int {
	count, err := zeq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (zeq *ZaloEmployeeQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, zeq.ctx, ent.OpQueryExist)
	switch _, err := zeq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (zeq *ZaloEmployeeQuery) ExistX(ctx context.Context) bool {
	exist, err := zeq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ZaloEmployeeQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (zeq *ZaloEmployeeQuery) Clone() *ZaloEmployeeQuery {
	if zeq == nil {
		return nil
	}
	return &ZaloEmployeeQuery{
		config:       zeq.config,
		ctx:          zeq.ctx.Clone(),
		order:        append([]zaloemployee.OrderOption{}, zeq.order...),
		inters:       append([]Interceptor{}, zeq.inters...),
		predicates:   append([]predicate.ZaloEmployee{}, zeq.predicates...),
		withEmployee: zeq.withEmployee.Clone(),
		// clone intermediate query.
		sql:  zeq.sql.Clone(),
		path: zeq.path,
	}
}

// WithEmployee tells the query-builder to eager-load the nodes that are connected to
// the "employee" edge. The optional arguments are used to configure the query builder of the edge.
func (zeq *ZaloEmployeeQuery) WithEmployee(opts ...func(*EmployeeQuery)) *ZaloEmployeeQuery {
	query := (&EmployeeClient{config: zeq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	zeq.withEmployee = query
	return zeq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		ZaloUID string `json:"zalo_uid"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ZaloEmployee.Query().
//		GroupBy(zaloemployee.FieldZaloUID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (zeq *ZaloEmployeeQuery) GroupBy(field string, fields ...string) *ZaloEmployeeGroupBy {
	zeq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ZaloEmployeeGroupBy{build: zeq}
	grbuild.flds = &zeq.ctx.Fields
	grbuild.label = zaloemployee.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		ZaloUID string `json:"zalo_uid"`
//	}
//
//	client.ZaloEmployee.Query().
//		Select(zaloemployee.FieldZaloUID).
//		Scan(ctx, &v)
func (zeq *ZaloEmployeeQuery) Select(fields ...string) *ZaloEmployeeSelect {
	zeq.ctx.Fields = append(zeq.ctx.Fields, fields...)
	sbuild := &ZaloEmployeeSelect{ZaloEmployeeQuery: zeq}
	sbuild.label = zaloemployee.Label
	sbuild.flds, sbuild.scan = &zeq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ZaloEmployeeSelect configured with the given aggregations.
func (zeq *ZaloEmployeeQuery) Aggregate(fns ...AggregateFunc) *ZaloEmployeeSelect {
	return zeq.Select().Aggregate(fns...)
}

func (zeq *ZaloEmployeeQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range zeq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, zeq); err != nil {
				return err
			}
		}
	}
	for _, f := range zeq.ctx.Fields {
		if !zaloemployee.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if zeq.path != nil {
		prev, err := zeq.path(ctx)
		if err != nil {
			return err
		}
		zeq.sql = prev
	}
	return nil
}

func (zeq *ZaloEmployeeQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ZaloEmployee, error) {
	var (
		nodes       = []*ZaloEmployee{}
		_spec       = zeq.querySpec()
		loadedTypes = [1]bool{
			zeq.withEmployee != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ZaloEmployee).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ZaloEmployee{config: zeq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, zeq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := zeq.withEmployee; query != nil {
		if err := zeq.loadEmployee(ctx, query, nodes, nil,
			func(n *ZaloEmployee, e *Employee) { n.Edges.Employee = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (zeq *ZaloEmployeeQuery) loadEmployee(ctx context.Context, query *EmployeeQuery, nodes []*ZaloEmployee, init func(*ZaloEmployee), assign func(*ZaloEmployee, *Employee)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*ZaloEmployee)
	for i := range nodes {
		fk := nodes[i].EmployeeID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(employee.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "employee_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (zeq *ZaloEmployeeQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := zeq.querySpec()
	_spec.Node.Columns = zeq.ctx.Fields
	if len(zeq.ctx.Fields) > 0 {
		_spec.Unique = zeq.ctx.Unique != nil && *zeq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, zeq.driver, _spec)
}

func (zeq *ZaloEmployeeQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(zaloemployee.Table, zaloemployee.Columns, sqlgraph.NewFieldSpec(zaloemployee.FieldID, field.TypeInt))
	_spec.From = zeq.sql
	if unique := zeq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if zeq.path != nil {
		_spec.Unique = true
	}
	if fields := zeq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, zaloemployee.FieldID)
		for i := range fields {
			if fields[i] != zaloemployee.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if zeq.withEmployee != nil {
			_spec.Node.AddColumnOnce(zaloemployee.FieldEmployeeID)
		}
	}
	if ps := zeq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := zeq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := zeq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := zeq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (zeq *ZaloEmployeeQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(zeq.driver.Dialect())
	t1 := builder.Table(zaloemployee.Table)
	columns := zeq.ctx.Fields
	if len(columns) == 0 {
		columns = zaloemployee.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if zeq.sql != nil {
		selector = zeq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if zeq.ctx.Unique != nil && *zeq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range zeq.predicates {
		p(selector)
	}
	for _, p := range zeq.order {
		p(selector)
	}
	if offset := zeq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := zeq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ZaloEmployeeGroupBy is the group-by builder for ZaloEmployee entities.
type ZaloEmployeeGroupBy struct {
	selector
	build *ZaloEmployeeQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (zegb *ZaloEmployeeGroupBy) Aggregate(fns ...AggregateFunc) *ZaloEmployeeGroupBy {
	zegb.fns = append(zegb.fns, fns...)
	return zegb
}

// Scan applies the selector query and scans the result into the given value.
func (zegb *ZaloEmployeeGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, zegb.build.ctx, ent.OpQueryGroupBy)
	if err := zegb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ZaloEmployeeQuery, *ZaloEmployeeGroupBy](ctx, zegb.build, zegb, zegb.build.inters, v)
}

func (zegb *ZaloEmployeeGroupBy) sqlScan(ctx context.Context, root *ZaloEmployeeQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(zegb.fns))
	for _, fn := range zegb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*zegb.flds)+len(zegb.fns))
		for _, f := range *zegb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*zegb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := zegb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ZaloEmployeeSelect is the builder for selecting fields of ZaloEmployee entities.
type ZaloEmployeeSelect struct {
	*ZaloEmployeeQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (zes *ZaloEmployeeSelect) Aggregate(fns ...AggregateFunc) *ZaloEmployeeSelect {
	zes.fns = append(zes.fns, fns...)
	return zes
}

// Scan applies the selector query and scans the result into the given value.
func (zes *ZaloEmployeeSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, zes.ctx, ent.OpQuerySelect)
	if err := zes.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ZaloEmployeeQuery, *ZaloEmployeeSelect](ctx, zes.ZaloEmployeeQuery, zes, zes.inters, v)
}

func (zes *ZaloEmployeeSelect) sqlScan(ctx context.Context, root *ZaloEmployeeQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(zes.fns))
	for _, fn := range zes.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*zes.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := zes.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
